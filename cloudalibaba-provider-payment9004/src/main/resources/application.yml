server:
  port: 9004

spring:
  application:
    name: nacos-payment-provider

  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848   #注册中心的地址


management:
  endpoints:
    web:
      exposure:
        include: '*'